<section class="benefits">
  <div class="benefits-inner">
    <div class="reveal">
      <div class="ba-wrap">
        <div class="before-after">
          <div class="ba-col before">
            <h4>❌ Avant</h4>
            <div class="ba-item"><span class="ba-icon">😤</span> 20 min à chercher une fiche</div>
            <div class="ba-item"><span class="ba-icon">😓</span> Rapports manuscrits illisibles</div>
            <div class="ba-item"><span class="ba-icon">😰</span> Litiges sans preuve</div>
            <div class="ba-item"><span class="ba-icon">🤯</span> Planning sur WhatsApp</div>
            <div class="ba-item"><span class="ba-icon">😩</span> Zéro visibilité activité</div>
            <div class="ba-item"><span class="ba-icon">⏰</span> 3h+ perdues/semaine</div>
          </div>
          <div class="ba-col after">
            <h4>✅ Après</h4>
            <div class="ba-item"><span class="ba-icon">⚡</span> Fiche trouvée en 2 sec</div>
            <div class="ba-item"><span class="ba-icon">📄</span> PDF envoyé automatiquement</div>
            <div class="ba-item"><span class="ba-icon">🛡️</span> Signature horodatée</div>
            <div class="ba-item"><span class="ba-icon">📅</span> Planning centralisé</div>
            <div class="ba-item"><span class="ba-icon">📊</span> Dashboard temps réel</div>
            <div class="ba-item"><span class="ba-icon">🎯</span> +3h/semaine récupérées</div>
          </div>
        </div>
        <div class="roi-banner">
          <div class="roi-item"><div class="roi-num">3h</div><div class="roi-lbl">Gagnées/sem.</div></div>
          <div class="roi-item"><div class="roi-num">2min</div><div class="roi-lbl">Par fiche</div></div>
          <div class="roi-item"><div class="roi-num">100%</div><div class="roi-lbl">Retrouvable</div></div>
          <div class="roi-item"><div class="roi-num">0€</div><div class="roi-lbl">Litige non doc.</div></div>
        </div>
      </div>
    </div>
    <div class="reveal benefits-text">
      <div class="section-eyebrow">Bénéfices concrets</div>
      <h2 class="section-title">Ce que vous gagnez vraiment</h2>
      <div class="benefits-list">
        <div class="benefit-row">
          <div class="benefit-ico">⏱️</div>
          <div class="benefit-text">
            <h4>3 heures récupérées par semaine</h4>
            <p>Plus de recherche de fiches, plus de rapports à rédiger. Vous gardez le temps pour votre vrai métier.</p>
            <span class="benefit-kpi">= 12h/mois × votre taux horaire</span>
          </div>
        </div>
        <div class="benefit-row">
          <div class="benefit-ico">💼</div>
          <div class="benefit-text">
            <h4>Image professionnelle premium</h4>
            <p>Vos clients reçoivent un rapport PDF avec votre logo dès la fin de l'intervention. Vous vous démarquez instantanément.</p>
            <span class="benefit-kpi">= Fidélisation + recommandations</span>
          </div>
        </div>
        <div class="benefit-row">
          <div class="benefit-ico">🛡️</div>
          <div class="benefit-text">
            <h4>Zéro litige non documenté</h4>
            <p>Chaque intervention est signée, horodatée, archivée. La preuve en 2 secondes si nécessaire.</p>
            <span class="benefit-kpi">= 1 litige évité = 500€+ économisés</span>
          </div>
        </div>
      </div>
      <div style="margin-top:24px">
        <a href="#inscription" class="btn-cta" style="display:inline-flex;font-size:14px;padding:13px 28px">Commencer à gagner du temps →</a>
      </div>
    </div>
  </div>
</section>

<style>
  .benefits {
    background: #fff;
    padding: 88px 48px;
    border-top: 1px solid var(--border);
  }

  .benefits-inner {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .before-after {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  .ba-col h4 {
    font-size: 13px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: .08em;
    margin-bottom: 14px;
    padding: 8px 14px;
    border-radius: 8px;
    text-align: center;
  }

  .ba-col.before h4 { background: #FEF2F2; color: #DC2626; }
  .ba-col.after h4 { background: var(--green-light); color: var(--green-dark); }

  .ba-item {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    font-size: 12px;
    margin-bottom: 10px;
    color: var(--text2);
    line-height: 1.4;
  }

  .ba-icon { font-size: 14px; flex-shrink: 0; }

  .roi-banner {
    margin-top: 20px;
    background: var(--blue);
    border-radius: 12px;
    padding: 18px 20px;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
  }

  .roi-item { text-align: center; }
  .roi-num { font-size: 26px; font-weight: 800; color: #fff; letter-spacing: -1px; }
  .roi-lbl { font-size: 11px; color: rgba(255, 255, 255, 0.65); }

  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: 18px;
    margin-top: 36px;
  }

  .benefit-row {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    padding: 16px;
    border: 1px solid var(--border);
    border-radius: 12px;
    background: var(--bg);
    transition: all .25s;
  }

  .benefit-row:hover {
    border-color: var(--blue-mid);
    background: #fff;
    transform: translateX(4px);
  }

  .benefit-ico {
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: var(--blue-light);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 18px;
  }

  .benefit-text h4 { font-size: 15px; font-weight: 700; color: var(--text); margin-bottom: 3px; }
  .benefit-text p { font-size: 13px; color: var(--text2); line-height: 1.5; }

  .benefit-kpi {
    font-size: 12px;
    font-weight: 700;
    color: var(--green-dark);
    background: var(--green-light);
    padding: 2px 9px;
    border-radius: 100px;
    margin-top: 5px;
    display: inline-block;
  }

  @media (max-width: 900px) {
    .benefits { padding: 64px 16px; }
    .benefits-inner { grid-template-columns: 1fr; gap: 40px; }
    .before-after { grid-template-columns: 1fr; gap: 10px; }
  }
</style>
