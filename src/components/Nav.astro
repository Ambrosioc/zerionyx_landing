<nav>
  <a href="#" class="nav-logo">
    <div class="nav-icon">
      <svg viewBox="0 0 18 18" fill="none">
        <path d="M2 4h14M2 9h9M2 14h11" stroke="white" stroke-width="2" stroke-linecap="round" />
        <circle cx="15" cy="14" r="3" fill="white" fill-opacity=".2" stroke="white" stroke-width="1.5" />
        <path d="M14 14l.9 1.1 1.7-1.8" stroke="white" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" />
      </svg>
    </div>
    <span class="nav-name">Zerio<span>nyx</span></span>
  </a>
  <div class="nav-links">
    <a href="#fonctionnalites">Fonctionnalités</a>
    <a href="#temoignages">Témoignages</a>
    <a href="#tarifs">Tarifs</a>
    <a href="#faq">FAQ</a>
  </div>
  <div class="nav-cta-wrap">
    <a href="#rdv" class="btn-rdv">📅 Prendre RDV</a>
    <a href="#inscription" class="btn-cta">Essai gratuit →</a>
  </div>
</nav>

<style>
  nav {
    position: sticky;
    top: 0;
    z-index: 200;
    background: rgba(255, 255, 255, 0.96);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
    padding: 0 48px;
    height: 66px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
  }

  .nav-icon {
    width: 34px;
    height: 34px;
    background: var(--blue);
    border-radius: 9px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .nav-icon svg { width: 18px; height: 18px; }

  .nav-name {
    font-size: 18px;
    font-weight: 800;
    color: var(--text);
    letter-spacing: -0.3px;
  }

  .nav-name span { color: var(--blue); }

  .nav-links {
    display: flex;
    gap: 28px;
  }

  .nav-links a {
    font-size: 14px;
    font-weight: 500;
    color: var(--text2);
    text-decoration: none;
    transition: color .2s;
  }

  .nav-links a:hover { color: var(--blue); }

  .nav-cta-wrap {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  @media (max-width: 900px) {
    nav { padding: 0 16px; }
    .nav-links { display: none; }
  }
</style>
