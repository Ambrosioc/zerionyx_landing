<div class="stats-bar">
  <div class="stat-cell reveal"><div class="stat-num">1.2M</div><div class="stat-lbl">Artisans & techniciens en France</div></div>
  <div class="stat-cell reveal"><div class="stat-num">80%</div><div class="stat-lbl">Encore sur papier ou Excel</div></div>
  <div class="stat-cell reveal"><div class="stat-num">2min</div><div class="stat-lbl">Pour une fiche complète signée</div></div>
  <div class="stat-cell reveal"><div class="stat-num">0€</div><div class="stat-lbl">Pendant toute la bêta</div></div>
</div>

<style>
  .stats-bar{
    position:relative;z-index:1;
    background:var(--surface);border-top:1px solid var(--border);border-bottom:1px solid var(--border);
    display:grid;grid-template-columns:repeat(4,1fr);
  }
  .stat-cell{
    padding:32px 20px;text-align:center;
    border-right:1px solid var(--border);transition:background .25s;
  }
  .stat-cell:last-child{border-right:none}
  .stat-cell:hover{background:rgba(255,255,255,0.9)}
  .stat-num{
    font-family:'Space Grotesk',sans-serif;
    font-size:40px;font-weight:700;letter-spacing:-2px;line-height:1;margin-bottom:5px;
    background:linear-gradient(135deg,var(--blue),var(--cyan));
    -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
  }
  .stat-lbl{font-size:13px;color:var(--text2)}

  @media(max-width:900px){
    .stats-bar{grid-template-columns:1fr 1fr}
    .stat-cell{border-right:none;border-bottom:1px solid var(--border)}
  }
</style>
